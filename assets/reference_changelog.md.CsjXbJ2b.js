import{H as e,ht as t,nt as n,tt as r}from"./chunks/framework.DiAOIirN.js";var i=JSON.parse(`{"title":"Changelog","description":"","frontmatter":{},"headers":[],"relativePath":"reference/changelog.md","filePath":"reference/changelog.md","lastUpdated":1788173818000}`),a={name:`reference/changelog.md`};function o(e,i,a,o,s,c){return t(),r(`div`,null,[...i[0]||=[n(`<h1 id="changelog" tabindex="-1">Changelog <a class="header-anchor" href="#changelog" aria-label="Permalink to “Changelog”">​</a></h1><h2 id="_0-1-0-2026-09-03" tabindex="-1">[0.1.0] - 2026-09-03 <a class="header-anchor" href="#_0-1-0-2026-09-03" aria-label="Permalink to “[0.1.0] - 2026-09-03”">​</a></h2><h3 id="added" tabindex="-1">Added <a class="header-anchor" href="#added" aria-label="Permalink to “Added”">​</a></h3><ul><li>Initial release: an MCP server that sends mail over SMTP, as the counterpart to <a href="https://github.com/ni-c/imap-mcp" target="_blank" rel="noreferrer">imap-mcp</a>, which deliberately cannot.</li><li>A multi-architecture container image at <code>ghcr.io/ni-c/smtp-mcp</code> (amd64 and arm64), published with an SBOM and build provenance. It runs as an unprivileged user and speaks stdio only, so it needs <code>-i</code> and exposes no port.</li><li>Seven tools. Four are always available and cannot put a message on the wire — <code>get_server_info</code>, <code>validate_recipients</code>, <code>preview_mail</code> and <code>test_connection</code>. Three send: <code>send_mail</code>, <code>reply_mail</code> and <code>forward_mail</code>.</li><li><strong><code>SMTP_ALLOW_SEND</code> defaults to <code>false</code>.</strong> Note the direction: the read/write variables elsewhere in this family guard changes to a system the operator already owns, while this one guards a channel out of it. Until it is set the sending tools are not registered at all.</li><li><strong><code>SMTP_ALLOWED_RECIPIENTS</code> is required to enable sending.</strong> An unset allowlist is refused at startup rather than treated as &quot;anyone&quot;; allowing everyone is spelled <code>*</code>.</li><li>Every send asks a human through MCP elicitation, falling back to a two-call token bound to a fingerprint of the recipients <em>and</em> the content. <code>ELICITATION=false</code> takes that fallback deliberately, for a deployment where a dialog is the wrong shape rather than an unwanted one — it never removes the guard, the fallback text then names this server rather than blaming the client, and a server started with it off says so on one startup line. The variable carries no prefix on purpose, so one export reaches every MCP server in the environment; this is the one where that costs the most. A value that is neither <code>true</code> nor <code>false</code> stops the server, like <code>SMTP_TLS</code> and unlike <code>SMTP_ALLOW_SEND</code>, because it is the only variable here that defaults to on.</li><li>The sender is fixed by <code>SMTP_FROM</code>. There is no <code>from</code> parameter.</li><li>Hourly send cap (<code>SMTP_MAX_SENDS_PER_HOUR</code>) and a per-message recipient cap (<code>SMTP_MAX_RECIPIENTS</code>).</li><li>Attachments are read only from <code>SMTP_ATTACHMENT_DIR</code>, behind an extension allowlist, an executable-extension refusal, a symlink refusal and a magic-byte check.</li><li>Outgoing HTML is sanitised: scripts, event handlers, remotely loaded images and unsafe URL schemes are removed, and every removal is reported rather than applied silently. Markup that is still dangerous after every pass has run — the case a regex cannot resolve and the recipient&#39;s parser can — <strong>refuses</strong> the message instead of repairing it, which is also what keeps the reported removals honest. &quot;Remotely loaded&quot; covers <code>src</code>, <code>srcset</code>, <code>imagesrcset</code>, <code>poster</code> and <code>background</code>, each <code>srcset</code> candidate on its own.</li><li><strong>At most once.</strong> A message the SMTP server accepted is remembered, by the same fingerprint the approval is bound to, for as long as an approval for it could still be redeemed; an identical send inside that window is answered with the earlier Message-ID instead of going out again. An approval proves that somebody agreed to a message, not that they agreed to it a second time — and a tool call is at-least-once by nature, so a client that times out and retries, a host that reconnects or a model that repeats itself would otherwise send a second copy. <code>send_mail</code> is the one operation in this family that is not idempotent. SECURITY.md records which protocol revision exposes a replayable approval, which one this server actually negotiates, and what to check the day that changes. The residual case — a connection lost after the end of <code>DATA</code>, where the outcome is genuinely unknown — keeps the rate-limit slot and writes an audit line saying so, rather than being covered over.</li><li>The confirmation dialog shows the <strong>body</strong>, and the quoted original and HTML part when set, each labelled with its length in characters. Every other layer binds the envelope; nothing looked at what the message said. The HTML part appears twice — as markup, and <strong>as the recipient reads it</strong>, derived from the sanitised source, because the first 200 characters of markup are mostly tags and on a message with an empty body the HTML <em>is</em> the message. The dialog also says when the plain-text body and the HTML part say different things, since most recipients see only the latter; <code>preview_mail</code> reports the same as <code>text_html_diverge</code>.</li><li>The injection detector&#39;s findings are kept per field, and the dialog says whose words matched: a quote that gives orders is a forwarded message trying to, a body that gives orders is the model writing them. <code>preview_mail</code> reports the fields as <code>suspicious_in</code>.</li><li>A quoted original is passed on unchanged; matches against known prompt-injection shapes are surfaced in the confirmation dialog instead.</li><li>A subject may not contain an RFC 2047 encoded-word (<code>=?utf-8?B?…?=</code>). It travels as ASCII and decodes to something else at the recipient, so the human would approve a subject other than the one that arrives.</li><li><code>get_server_info</code> reports <code>elicitation_enabled</code> and a <code>confirmation</code> field that follows the configuration, so it never claims a human is asked when <code>ELICITATION=false</code> says otherwise.</li><li>Every accepted message is recorded on stderr, and optionally in <code>SMTP_AUDIT_LOG</code>. Never the body. <strong>Refusals are logged too</strong> — a refusal by the allowlist or the attachment checks, a declined dialog and a rejected token each write a line marked <code>outcome=refused</code>, <code>declined</code> or <code>token_rejected</code>, with the recipients and subject that were asked for. A log of what went out cannot show that a session was being steered; the refusals can. Audit lines quote every string value and every array element, always, so a subject like <code>Invoice_bcc=[quiet@evil.example]</code> cannot hand a parser a <code>bcc</code> field that never existed.</li><li>Every tool declares an <code>outputSchema</code> and answers with <code>structuredContent</code> beside the text block, so a client does not have to parse prose to use a result. <code>preview_mail</code> carries the untrusted marker as <code>untrusted: true</code> and <code>source: &quot;smtp&quot;</code> fields — a quoted original was written by whoever sent it, and anyone in the world can send mail. Its text block keeps the nonce fence; the structured half states the same fields rather than making a client parse it. No other tool carries the marker, because no other tool reports anything this server did not write itself.</li><li><code>SMTP_ALLOW_TOOLS</code> / <code>SMTP_DENY_TOOLS</code> narrow the tool list; <code>essential</code> selects a curated five.</li><li>An integration suite in <code>test/integration/</code> that drives the built server over real stdio against a throwaway Mailpit container, sends through it and reads the message back to check what actually arrived.</li><li>stdio is served through <code>serveStdio</code>, so the connection&#39;s protocol era is negotiated on the opening exchange rather than assumed, and a client that pins either revision is served the one it asked for.</li><li>The tool filter, the approval flow, the host classifier and the documentation-asset generator come from <strong><code>mcp-tool-allowlist</code></strong>, <strong><code>mcp-approval</code></strong>, <strong><code>mcp-internal-hosts</code></strong> and <strong><code>svg-asset-set</code></strong> rather than from copies kept here. The approval flow in particular was written here and in imap-mcp, and became a library once the two had grown near-identical copies of it. None of the packages has a runtime dependency of its own, so this makes the tree smaller rather than larger. The server runs on <strong>MCP SDK 2.0</strong> and is built with <strong>oxlint</strong> and <strong>TypeScript 7</strong>.</li></ul><h3 id="security" tabindex="-1">Security <a class="header-anchor" href="#security" aria-label="Permalink to “Security”">​</a></h3><ul><li><strong>Every recipient is checked twice, and the envelope comes from the checked lists.</strong> A local part is an RFC 5322 dot-atom and a domain is ASCII, because <code>ceo,anna@work.example</code> has one allowlisted domain to a naive parser and becomes <em>two</em> RCPT commands at the SMTP server — the first a bare <code>ceo</code> that a submission relay qualifies with its own domain. The message is composed once and sent as raw bytes with an envelope built from the address lists that passed the allowlist, never from the headers, so delivery follows what was approved.</li><li><strong>The HTML sanitiser models the tokenizer, not a space.</strong> Attribute patterns start after whitespace, <code>/</code> or a quote rather than requiring whitespace; scheme and remote checks run on the <strong>decoded</strong> value, with character references and CSS escapes resolved and backslashes read as slashes; and the final refusal check is built from the same element list as the removal passes. A check that runs on the raw string is checking something no recipient&#39;s parser will ever see. The passes are linear rather than merely bounded, and the HTML input is capped, so <code>preview_mail</code> — which is reachable with no send gate, no confirmation and no rate limit — cannot hold the event loop.</li><li><strong>Every recipient and every attachment gets a line of its own in the dialog</strong>, numbered <code>i/N</code>. A detail value is cut at 200 characters and six ordinary addresses already exceed that, so a single line per field showed the first few and dropped the rest without saying so — and the one that goes missing is the one that matters, because an address appended to Bcc is invisible in the delivered message too.</li><li><strong>A declined dialog costs a slot of <code>SMTP_MAX_SENDS_PER_HOUR</code>.</strong> The quota is not only a cap on messages sent; it is the only bound on how many times a person can be asked, and a free decline is an unlimited supply of dialogs — the same message reworded until somebody agrees out of tiredness. A token that does not match gives the slot back, because nobody decided anything and the alternative would let a caller burn the hour with invented tokens.</li><li><strong><code>preview_mail</code> answers inside the same result budget as every other tool.</strong> It is the one tool that returns a whole message and the one reachable with no send gate, no confirmation and no rate limit, and it had no budget at all: a 64 kB HTML part with a distinct unsafe URL scheme per link produced 366 kB across the two channels. The list of removals is capped and says how many are not named, and it is reported as data rather than in the server&#39;s own voice — a scheme is whatever the caller wrote before a colon, and that text used to sit outside the untrusted fence.</li><li><strong>The confirmation dialog cannot be forged from its own contents.</strong> Recipients, subject, Bcc and body each stand on their own labelled line rather than inside a sentence the server wrote, and the line-breaking codepoints that would fake such a line under <code>white-space: pre-wrap</code> (U+2028, U+2029, U+000B, U+000C, U+0085) are rejected — refusing CR and LF is not enough. Caller-derived text stays out of the dialog&#39;s <code>what</code> and <code>consequence</code>, which are not capped by the approval library, and appears only in details, which are.</li><li><strong>An attachment is checked on the handle it is read from.</strong> The file is opened non-blocking, so a FIFO substituted between <code>lstat</code> and <code>open</code> cannot hang the handler; type and size are checked again on the open handle; and the read is capped at the size that handle reported.</li><li><strong>A message that cannot fit is refused before it is built.</strong> Ten attachments at the default ceiling are 67 MB once base64 has run over them; a lower bound is taken before composing, so the oversize case is refused rather than assembled and then rejected. A result too large to shrink is an error rather than an envelope carrying an oversized document.</li><li><strong>Defaults leave out the attachment types that carry their own execution.</strong> <code>text/html</code> opens in a browser with none of a mail client&#39;s restrictions and none of the sanitising the HTML <em>part</em> of a message gets; <code>application/zip</code> passes the magic-byte check on its own bytes and can carry the executable that check never sees. Both remain available through <code>SMTP_ATTACHMENT_TYPES</code>.</li><li><strong>The rate limit reserves rather than checks.</strong> MCP clients call in parallel, and checking first and counting after leaves the whole SMTP round trip open as a window.</li><li><strong>The approval fingerprint binds what was actually approved</strong>: the arguments rather than the composed bytes, which carry a <code>Date</code> and a random Message-ID; the To/Cc/Bcc <em>split</em> rather than the multiset of addresses, so an approved visible recipient cannot be moved into Bcc afterwards; and the attachment bytes, so the file cannot be swapped between the two token calls.</li><li>Both credentials are removed from <code>process.env</code> once the configuration is read, not just the password: a user name is half a credential and, for most providers, the mailbox address. An upstream error that is an HTML page is dropped however it is prefixed — nodemailer appends the server&#39;s response to its own message, so a captive portal on the submission port arrives as <code>Invalid login: &lt;html&gt;…</code> rather than as markup at position zero. <code>test_connection</code> closes the session it opens instead of leaving an authenticated connection pooled for the lifetime of the process.</li><li>A Message-ID, <code>in_reply_to</code> or <code>references</code> entry is printable ASCII without whitespace or angle brackets. nodemailer writes these three headers byte for byte, so a non-ASCII character was an 8-bit header on the wire and U+2028 quietly split one identifier into two.</li><li>The advertised schemas avoid a spelling that is legal JSON Schema and still gets a tool refused, or its constraint silently dropped, by some MCP clients: a nullable field is written as <code>anyOf</code> branches rather than a two-entry type array. The two-call <code>confirm_token</code> prompt is an error result, which is what &quot;the message was not sent&quot; means and what lets a tool that declares an output schema answer without <code>structuredContent</code>.</li></ul>`,6)]])}var s=e(a,[[`render`,o]]);export{i as __pageData,s as default};